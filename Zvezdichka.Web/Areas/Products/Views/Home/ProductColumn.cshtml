@using Zvezdichka.Web.Infrastructure.Constants
@model Zvezdichka.Web.Areas.Products.Models.ProductListingViewModel

<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 product-column-wrapper">
    <div class="product-column">
        <div class="product-column-title-wrapper">
            <p>
                <a asp-route="@WebConstants.Routes.ProductDetails" asp-route-id="@Model.Id" asp-route-title="@Model.Name" class="product-column-title">@Model.Name</a>
            </p>
        </div>

        <div id="thumbnail-container">
            <a class="d-block mb-4 h-100" asp-route="@WebConstants.Routes.ProductDetails" asp-route-id="@Model.Id" asp-route-title="@Model.Name">
                <img id="thumbnail" src="@Model.ThumbnailSource" class="img-responsive img-thumbnail" alt="@Model.Name">
            </a>
        </div>

        <span class="product-column-price">&#36 @Html.DisplayFor(modelItem => Model.Price)</span>

        @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
        {
            <div>
                <span>In Stock: @Model.Stock</span>
            </div>
            <div class="row">
                <a asp-action="Edit" asp-route-id="@Model.Id"
                   class="btn btn-default col-md-5 col-md-offset-1 col-sm-5 col-sm-offset-1 col-xs-4 col-xs-offset-2">
                    Edit
                </a>
                <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger col-md-5 col-sm-5 col-xs-4">Delete</a>
            </div>
        }
    </div>
</div>